name: Azure - functions

on: 
  workflow_dispatch:
    inputs:
      AZURE_FUNCTIONAPP_NAME: 
        type: string
        description: 'Azure Function App name'
        required: true
  push:
    branches:
    - 'main'
    paths:
    - 'azure/functions/src/**' 
    - 'azure/functions/Cargo.toml'
    - 'azure/functions/nxfutil**/function.json'      
    - 'azure/functions/host.json'
    
jobs:      
  azureFunctions:
    uses: ./.github/workflows/reusable-azure-functions.yml
    with:
      AZURE_FUNCTIONAPP_NAME: ${{ inputs.AZURE_FUNCTIONAPP_NAME }}
    secrets: inherit    